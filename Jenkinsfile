pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Checking out AI Dashboard Builder'
            }
        }

        stage('Build') {
            steps {
                echo 'Build completed'
            }
        }

        stage('Test') {
            steps {
                echo 'Tests completed'
            }
        }
    }
}